# Python Scripts for Algorithms and Testing

## Часть 1: Алгоритмы
Этот скрипт выполняет следующие задачи:
1. Читает список чисел от пользователя и выводит только четные числа.
2. Находит и выводит максимальное и минимальное число из списка.
3. Сортирует список в порядке возрастания без использования встроенной функции `sorted()`.

### Как запустить:
1. Скачайте скрипт `algorithms.py`.
2. Запустите команду:
   ```bash
   python algorithms.py


## Часть 2 Автоматизация тестирования с использованием Selenium
Этот скрипт автоматизирует тестирование веб-страницы https://example.com с использованием Selenium:

Открывает страницу.

Проверяет, что заголовок содержит слово "Example".

Кликает по ссылке с текстом "More information".

Проверяет перенаправление на страницу с URL https://www.iana.org/help/example-domains.

Как запустить:
Установите Selenium:

bash
Копировать
Редактировать
pip install selenium
Скачайте драйвер для вашего браузера (например, ChromeDriver) и убедитесь, что он доступен в PATH.

Запустите тест:

bash
Копировать
Редактировать
python selenium_test.py

Требования:
Python 3.x
Selenium